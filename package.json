{
  "name": "editora-globo-dashboard",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": "22.x",
    "npm": ">=10"
  },
  "workspaces": [
    "apps/web-dashboard"
  ],
  "scripts": {
    "build": "npm --prefix apps/web-dashboard run build",
    "install:web": "npm --prefix apps/web-dashboard install",
    "dev":     "npm --prefix apps/web-dashboard run dev"
  }
}
